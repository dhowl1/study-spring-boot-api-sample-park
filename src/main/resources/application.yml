spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3307/demo_jwt

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:/db/migration 

